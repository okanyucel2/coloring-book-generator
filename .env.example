# Required for Etsy integration
ETSY_API_KEY=
ETSY_API_SECRET=
ETSY_REDIRECT_URI=http://localhost:5000/api/v1/etsy/callback

# Optional: Database (defaults to SQLite)
# DATABASE_URL=sqlite+aiosqlite:///./coloring_book_api.db

# Auth (all required for Google OAuth — app works without these, auth just won't be mounted)
JWT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FRONTEND_URL=http://localhost:5173
# BACKEND_URL=http://localhost:5000  # Optional: explicit backend URL for OAuth callbacks

# Provider token encryption (Fernet key — generate with: python -c "from auth_core import TokenEncryption; print(TokenEncryption.generate_key())")
# Auto-generated in dev if empty (tokens lost on restart)
PROVIDER_TOKEN_KEY=
