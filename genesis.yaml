backend:
  command: "python -m uvicorn coloring_book.api.app:app --host 0.0.0.0 --port ${PORT}"
  env:
    PYTHONPATH: src
  health_check: "/api/health"
  ready_timeout: 10

frontend:
  command: "npx vite --host 0.0.0.0 --port ${PORT}"
  cwd: frontend
