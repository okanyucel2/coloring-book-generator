# Backend API target (for Vite dev proxy)
VITE_API_TARGET=http://127.0.0.1:10159

# Note: Google OAuth env vars (JWT_SECRET, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET,
# FRONTEND_URL) are set on the BACKEND, not the frontend.
# See ../backend/.env or ../.env.example for backend configuration.
